import Footer from './footer.html';
import './footer.scss';
import htmlToElement from '../../../utils';
import { PageElement } from '../../../types';

const footer: PageElement = htmlToElement(Footer);

export default footer;
