import Main from './main.html';
import './main.scss';
import htmlToElement from '../../../utils';
import { PageElement } from '../../../types';

const main: PageElement = htmlToElement(Main);

export default main;
